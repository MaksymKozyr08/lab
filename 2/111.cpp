#include <iostream>
#include <queue>
#include <vector>
#include <chrono> 
using namespace std;

typedef long double ld;
typedef long long ll;


int main(){
    ll n;
    cin>>n;
    vector<string> a(n);
    for(auto i=0;i<n;++i){
        a.push_back(i);
    }
    return 0;
}